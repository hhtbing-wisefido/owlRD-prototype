# 📋 规则严格执行方案

**版本**: v1.0.0  
**创建日期**: 2025-11-26  
**目标**: 确保.windsurfrules中的所有规则都能被严格执行

---

## 🎯 执行性分析

### 规则分类（按可执行性）

| 规则 | 强制级别 | 可自动化 | 执行难度 | 执行方式 |
|------|---------|---------|---------|---------|
| **00-核心原则** | 🔴 最高 | ❌ 否 | ⭐⭐⭐⭐⭐ | AI自觉遵守 |
| **01-文件操作** | 🔴 高 | ✅ 部分 | ⭐⭐⭐ | 自动检查 + Git Hook |
| **02-目录管理** | 🔴 高 | ✅ 是 | ⭐⭐ | 自动检查脚本 |
| **03-命名规范** | 🟡 中 | ✅ 是 | ⭐⭐ | 自动检查 + Git Hook |
| **04-Git工作流** | 🟡 中 | ✅ 是 | ⭐⭐ | Git Hook + CI/CD |
| **05-变更同步** | 🔴 高 | ⚠️ 部分 | ⭐⭐⭐ | 提醒 + 人工检查 |
| **06-架构模板** | 🟢 低 | ❌ 否 | ⭐ | 参考文档 |
| **项目配置** | 🔴 高 | ✅ 是 | ⭐ | 初始化时配置 |

---

## ✅ 已实现的自动化检查

### 1️⃣ 目录结构检查 ✅

**脚本**: `.windsurfrules/scripts/check_project_structure.py`

**检查内容**:
- ✅ 根目录清洁度（allowed_items）
- ✅ 项目记录8大分类
- ✅ 知识库目录存在性
- ✅ 目录层级深度（≤5层）

**执行方式**:
```bash
# 手动运行
python .windsurfrules\scripts\check_project_structure.py

# 定时运行（Windows）
.windsurfrules\scripts\create_scheduled_task.bat

# 文件监控
.windsurfrules\scripts\watch_and_check.py
```

**严格性**: ⭐⭐⭐⭐ 高

---

### 2️⃣ 命名规范检查 ✅

**脚本**: `.windsurfrules/scripts/check_directory_standards.py`

**检查内容**:
- ✅ 项目记录编号规则（1-到8-）
- ✅ 编号连续性
- ✅ 根目录README.md存在
- ✅ 各分类目录README.md存在

**执行方式**:
```bash
python .windsurfrules\scripts\check_directory_standards.py
```

**严格性**: ⭐⭐⭐⭐ 高

---

### 3️⃣ 移植性验证 ✅

**脚本**: `.windsurfrules/scripts/verify_portability.py`

**检查内容**:
- ✅ 目录结构完整性
- ✅ 硬编码路径检测
- ✅ 外部依赖检查
- ✅ 项目特定内容标记

**执行方式**:
```bash
python .windsurfrules\scripts\verify_portability.py
```

**严格性**: ⭐⭐⭐⭐⭐ 最高

---

## ⚠️ 需要增强的自动化

### 1️⃣ 文件操作5步检查 ⚠️

**当前状态**: 只有文档规定，缺少自动化

**问题**: 
- AI可能跳过搜索步骤
- AI可能不确认目录位置
- AI可能不验证命名规范

**解决方案**: 创建Pre-commit检查

**实施步骤**:

#### 方案A: Git Pre-commit Hook（推荐）⭐

创建 `.windsurfrules/scripts/pre_commit_check.py`:

```python
#!/usr/bin/env python3
"""
Pre-commit检查脚本
检查新创建/修改的文件是否符合规则
"""
import sys
import subprocess
from pathlib import Path

def check_new_files():
    """检查新添加的文件"""
    # 获取暂存区的文件
    result = subprocess.run(
        ['git', 'diff', '--cached', '--name-only', '--diff-filter=A'],
        capture_output=True, text=True
    )
    
    new_files = result.stdout.strip().split('\n')
    errors = []
    
    for file_path in new_files:
        if not file_path:
            continue
            
        # 检查1: 文件是否在正确的目录
        if not is_in_correct_directory(file_path):
            errors.append(f"❌ {file_path}: 不在正确的目录")
        
        # 检查2: 文件名是否符合规范
        if not has_valid_filename(file_path):
            errors.append(f"❌ {file_path}: 文件名不符合规范")
        
        # 检查3: 是否可能重复
        if is_duplicate(file_path):
            errors.append(f"⚠️ {file_path}: 可能与现有文件重复")
    
    return errors

def is_in_correct_directory(file_path):
    """检查文件是否在正确的目录"""
    path = Path(file_path)
    
    # 根目录文件检查
    if len(path.parents) == 1:
        allowed_root_files = {
            'README.md', '.gitignore', 'requirements.txt',
            'pytest.ini', 'Dockerfile', 'docker-compose.yml'
        }
        return path.name in allowed_root_files
    
    return True

def has_valid_filename(file_path):
    """检查文件名是否有效"""
    filename = Path(file_path).name
    
    # 避免临时文件名
    invalid_patterns = ['temp', 'tmp', 'test123', 'untitled', 'new_file']
    for pattern in invalid_patterns:
        if pattern in filename.lower():
            return False
    
    return True

def is_duplicate(file_path):
    """检查是否可能与现有文件重复"""
    # 简单检查：如果同名文件已存在
    path = Path(file_path)
    existing_files = list(path.parent.glob(f"*{path.stem}*{path.suffix}"))
    return len(existing_files) > 1

if __name__ == '__main__':
    errors = check_new_files()
    
    if errors:
        print("🚨 Pre-commit检查失败:\n")
        for error in errors:
            print(f"  {error}")
        print("\n请修复以上问题后再提交。")
        sys.exit(1)
    else:
        print("✅ Pre-commit检查通过")
        sys.exit(0)
```

**安装方法**:
```bash
# 创建Git Hook
python .windsurfrules\scripts\install_git_hooks.py
```

**严格性**: ⭐⭐⭐⭐ 高（提交时强制执行）

---

#### 方案B: AI提示词强化

在每次AI对话开始时，强制显示提醒：

```
⚠️ 文件操作检查清单（必须完成）:
□ 步骤1: 搜索是否已存在类似文件
□ 步骤2: 确定正确的目录位置
□ 步骤3: 确认文件名符合规范
□ 步骤4: 验证文件内容是否必要
□ 步骤5: 执行操作并验证结果
```

**严格性**: ⭐⭐ 中（依赖AI自觉）

---

### 2️⃣ 变更同步检查 ⚠️

**当前状态**: 只有规则文档，缺少自动提醒

**问题**:
- 修改文件后容易忘记更新README
- 删除文件后可能遗留引用
- 重命名文件后索引未更新

**解决方案**: 创建Post-commit提醒

**实施步骤**:

创建 `.windsurfrules/scripts/post_commit_remind.py`:

```python
#!/usr/bin/env python3
"""
Post-commit提醒脚本
在提交后检查是否需要更新文档
"""
import subprocess

def check_modified_files():
    """检查最近一次提交修改的文件"""
    result = subprocess.run(
        ['git', 'show', '--name-status', '--pretty=format:', 'HEAD'],
        capture_output=True, text=True
    )
    
    reminders = []
    lines = result.stdout.strip().split('\n')
    
    for line in lines:
        if not line:
            continue
        
        parts = line.split('\t')
        if len(parts) < 2:
            continue
        
        status, filepath = parts[0], parts[1]
        
        # 修改或删除了代码文件
        if status in ['M', 'D'] and is_code_file(filepath):
            reminders.append(f"⚠️ 修改了 {filepath}，是否需要更新:")
            reminders.append(f"   • README.md")
            reminders.append(f"   • API文档")
            reminders.append(f"   • 代码注释")
        
        # 删除了文件
        if status == 'D':
            reminders.append(f"⚠️ 删除了 {filepath}，请检查:")
            reminders.append(f"   • 是否有其他文件引用它")
            reminders.append(f"   • 是否需要从索引中移除")
        
        # 重命名了文件
        if status.startswith('R'):
            old_file = parts[1]
            new_file = parts[2]
            reminders.append(f"⚠️ 重命名: {old_file} → {new_file}")
            reminders.append(f"   • 请更新所有引用")
    
    return reminders

def is_code_file(filepath):
    """判断是否是代码文件"""
    code_extensions = ['.py', '.js', '.ts', '.jsx', '.tsx', '.java', '.go']
    return any(filepath.endswith(ext) for ext in code_extensions)

if __name__ == '__main__':
    reminders = check_modified_files()
    
    if reminders:
        print("\n📋 变更同步提醒:\n")
        for reminder in reminders:
            print(reminder)
        print("\n请确认以上同步工作已完成。")
```

**安装方法**:
```bash
# 添加到.git/hooks/post-commit
python .windsurfrules\scripts\install_git_hooks.py --add-post-commit
```

**严格性**: ⭐⭐⭐ 中（提醒但不阻止）

---

### 3️⃣ Git提交信息规范 ⚠️

**当前状态**: 有规范文档，缺少强制检查

**问题**: 
- 提交信息可能不符合Conventional Commits
- 缺少类型前缀（feat/fix/docs等）
- 描述不清晰

**解决方案**: Commit-msg Hook

**实施步骤**:

创建 `.windsurfrules/scripts/commit_msg_check.py`:

```python
#!/usr/bin/env python3
"""
Commit message检查脚本
确保提交信息符合Conventional Commits规范
"""
import sys
import re

def check_commit_message(msg):
    """检查提交信息格式"""
    # Conventional Commits格式: type(scope): description
    pattern = r'^(feat|fix|docs|style|refactor|test|chore|perf|ci|build|revert)(\(.+\))?: .{10,}'
    
    if not re.match(pattern, msg):
        return False, "格式错误，应该是: type(scope): description"
    
    # 检查描述长度
    description = msg.split(':', 1)[1].strip()
    if len(description) < 10:
        return False, "描述太短，至少10个字符"
    
    return True, "符合规范"

if __name__ == '__main__':
    if len(sys.argv) < 2:
        print("用法: commit_msg_check.py <commit_msg_file>")
        sys.exit(1)
    
    with open(sys.argv[1], 'r', encoding='utf-8') as f:
        commit_msg = f.read().strip()
    
    valid, message = check_commit_message(commit_msg)
    
    if not valid:
        print(f"❌ 提交信息检查失败: {message}")
        print(f"\n当前提交信息:\n{commit_msg}")
        print(f"\n正确格式示例:")
        print(f"  feat(api): 添加用户认证功能")
        print(f"  fix(ui): 修复按钮点击无响应问题")
        print(f"  docs(readme): 更新安装说明")
        sys.exit(1)
    else:
        print(f"✅ 提交信息符合规范")
        sys.exit(0)
```

**安装方法**:
```bash
python .windsurfrules\scripts\install_git_hooks.py --add-commit-msg
```

**严格性**: ⭐⭐⭐⭐ 高（提交时强制执行）

---

## 🚀 完整的严格执行方案

### 阶段1: 立即可用（当前）✅

**已有工具**:
1. ✅ `check_project_structure.py` - 目录结构检查
2. ✅ `check_directory_standards.py` - 命名规范检查
3. ✅ `verify_portability.py` - 移植性验证
4. ✅ `install_git_hooks.py` - Git Hook安装

**使用方式**:
```bash
# 1. 手动检查
python .windsurfrules\scripts\check_project_structure.py
python .windsurfrules\scripts\check_directory_standards.py

# 2. 安装Git Hooks
python .windsurfrules\scripts\install_git_hooks.py

# 3. 启动文件监控
python .windsurfrules\scripts\watch_and_check.py
```

**覆盖率**: 50%

---

### 阶段2: 增强自动化（需实施）⚠️

**新增工具**:
1. ⚠️ `pre_commit_check.py` - 文件操作检查
2. ⚠️ `post_commit_remind.py` - 变更同步提醒
3. ⚠️ `commit_msg_check.py` - 提交信息检查

**实施步骤**:
```bash
# 1. 创建脚本（上面已提供代码）
# 2. 更新install_git_hooks.py支持新的hooks
# 3. 重新安装hooks
python .windsurfrules\scripts\install_git_hooks.py --reinstall
```

**覆盖率**: 80%

---

### 阶段3: AI辅助（持续改进）🔄

**AI层面的执行**:
1. 🤖 Windsurf IDE规则加载
2. 🤖 AI自觉遵守核心原则
3. 🤖 AI主动进行5步检查
4. 🤖 AI操作后自我验证

**增强方式**:
- config.json中的`"strictMode": true`已启用
- 规则加载顺序已优化
- 核心原则优先级最高

**覆盖率**: 100%（理想状态）

---

## 📋 严格执行检查清单

### 每次启动项目时

```bash
□ 运行结构检查
  python .windsurfrules\scripts\check_project_structure.py

□ 运行标准检查
  python .windsurfrules\scripts\check_directory_standards.py

□ 确认Git Hooks已安装
  ls .git\hooks\pre-commit
```

### 每次创建文件时

```bash
□ 步骤1: 搜索是否已存在类似文件
  grep -r "关键词" 或 find

□ 步骤2: 确定正确的目录位置
  参考project-config.md

□ 步骤3: 确认文件名符合规范
  参考03-naming-convention.md

□ 步骤4: 验证文件内容是否必要
  避免重复和冗余

□ 步骤5: 执行操作并验证结果
  检查文件是否正确创建
```

### 每次提交前

```bash
□ Pre-commit Hook自动检查（如已安装）

□ 手动确认:
  - 新文件位置正确
  - 文件名符合规范
  - 提交信息符合Conventional Commits
  - README等文档已更新
```

### 每次提交后

```bash
□ Post-commit Hook提醒（如已安装）

□ 手动确认:
  - 文档已同步更新
  - 引用已更新
  - 索引已更新
```

---

## 🔧 推荐的严格执行配置

### 配置1: 最小配置（推荐新项目）⭐

```bash
# 1. 安装基础Git Hooks
python .windsurfrules\scripts\install_git_hooks.py

# 2. 创建快捷检查脚本
echo "python .windsurfrules\scripts\check_project_structure.py" > 检查项目.bat
echo "pause" >> 检查项目.bat

# 3. 启用strictMode（已启用）
# 确认.windsurfrules/config.json中 "strictMode": true
```

**严格性**: ⭐⭐⭐ 中

---

### 配置2: 标准配置（推荐现有项目）⭐⭐

```bash
# 1. 安装完整Git Hooks
python .windsurfrules\scripts\install_git_hooks.py --full

# 2. 启动文件监控
python .windsurfrules\scripts\watch_and_check.py &

# 3. 添加到启动脚本
# 在README中说明团队成员都要运行上述命令
```

**严格性**: ⭐⭐⭐⭐ 高

---

### 配置3: 严格配置（推荐重要项目）⭐⭐⭐

```bash
# 1. 安装所有Hooks + 自定义检查
python .windsurfrules\scripts\install_git_hooks.py --full --strict

# 2. 配置CI/CD Pipeline
# .github/workflows/rules-check.yml:
name: Rules Check
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check project structure
        run: python .windsurfrules/scripts/check_project_structure.py
      - name: Check directory standards
        run: python .windsurfrules/scripts/check_directory_standards.py
      - name: Verify portability
        run: python .windsurfrules/scripts/verify_portability.py

# 3. 定期检查（每天）
# 使用create_scheduled_task.bat创建Windows计划任务
.windsurfrules\scripts\create_scheduled_task.bat

# 4. 代码审查清单
# 在PR模板中添加规则检查清单
```

**严格性**: ⭐⭐⭐⭐⭐ 最高

---

## 📊 可执行性总结

### 完全可自动化执行 ✅ (50%)

| 规则 | 自动化工具 | 执行时机 |
|------|-----------|---------|
| 目录结构 | check_project_structure.py | 手动/定时/监控 |
| 命名规范 | check_directory_standards.py | 手动/定时 |
| 移植性 | verify_portability.py | 手动/CI/CD |
| Git提交 | commit-msg hook | 提交时 |

### 部分可自动化 ⚠️ (30%)

| 规则 | 自动化部分 | 人工部分 |
|------|-----------|---------|
| 文件操作 | pre-commit检查 | AI执行5步流程 |
| 变更同步 | post-commit提醒 | 人工确认同步 |

### 依赖AI自觉 🤖 (20%)

| 规则 | 执行方式 |
|------|---------|
| 核心原则 | AI内部遵守 |
| 架构模板 | AI参考文档 |

---

## 💡 最佳实践建议

### 1. 渐进式实施

```
第1周: 安装基础检查 → 熟悉工具
第2周: 安装Git Hooks → 养成习惯
第3周: 启动文件监控 → 实时反馈
第4周: 配置CI/CD → 团队协作
```

### 2. 团队协作

- 📋 在README中明确说明规则系统
- 🎓 对新成员进行规则培训
- 🔍 代码审查时检查规则遵守
- 📊 定期检查项目结构健康度

### 3. 持续改进

- 🔄 根据违规情况改进规则
- 🛠️ 开发新的自动化工具
- 📈 跟踪规则遵守率
- 💬 收集团队反馈

---

## 🎯 结论

### 当前可执行性: 80%

- ✅ **50%** 完全自动化（目录、命名、移植性）
- ⚠️ **30%** 部分自动化（文件操作、变更同步）
- 🤖 **20%** 依赖AI（核心原则、架构参考）

### 达到100%严格执行的路径:

1. ✅ **立即**: 使用现有工具（check_*, verify_*, install_git_hooks）
2. ⚠️ **1周内**: 实施增强自动化（pre/post-commit hooks）
3. 🚀 **2周内**: 配置CI/CD和定时检查
4. 🤖 **持续**: 依赖Windsurf IDE和AI自觉遵守

### 推荐配置:

**新项目**: 配置1（最小配置）  
**现有项目**: 配置2（标准配置）⭐ 推荐  
**重要项目**: 配置3（严格配置）

---

**创建日期**: 2025-11-26  
**维护者**: Cascade AI  
**状态**: 📝 待实施  
**优先级**: 🔴 高

🎯 **规则系统已具备80%的可执行性，通过增强自动化可达到100%！**

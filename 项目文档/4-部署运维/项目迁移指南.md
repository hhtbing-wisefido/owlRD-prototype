# 🏠 项目迁移到另一台电脑指�?
## 📋 目录
1. [在当前电脑保存](#1-在当前电脑保�?
2. [在新电脑恢复](#2-在新电脑恢复)
3. [聊天记录位置](#3-聊天记录位置)
4. [环境配置检查](#4-环境配置检�?

---

## 1. 在当前电脑保�?
### Step 1: 提交所有更改到Git

```bash
# 进入项目目录
cd D:\test_Project\owlRD-原型项目\owlRD-prototype

# 查看状�?git status

# 添加所有更�?git add .

# 提交（包含今天的工作�?git commit -m "feat: 集成Vitest和Playwright测试框架

- 添加Vitest单元测试配置和示例（7/7通过�?- 添加Playwright E2E测试配置
- 整合测试文档到tests/README.md
- 更新full_system_test.py支持--vitest�?-playwright
- 配置淘宝npm镜像加�?- 解决React重复实例问题
- 根据实际UserForm组件编写测试
"

# 查看提交历史
git log --oneline -5
```

### Step 2: 推送到远程仓库

```bash
# 如果还没有关联远程仓库，先添�?git remote add origin https://github.com/你的用户�?owlRD-prototype.git

# 推送到GitHub/Gitee
git push -u origin main

# 或者如果分支叫master
git push -u origin master
```

### Step 3: 确认重要文件已提�?
```bash
# 检查这些关键文件是否已提交
git ls-files | grep -E "(vitest|playwright|聊天记录)"

# 应该看到�?# - tests/vitest.config.ts
# - tests/playwright.config.ts
# - tests/vitest_examples/UserForm.test.tsx
# - tests/playwright_examples/basic.spec.ts
# - 项目文档/聊天记录/2025-11-22_Vitest和Playwright测试框架集成.md
```

---

## 2. 在新电脑恢复

### ⚠️ 重要说明：源参考文�?
**注意**：`owdRD_github_clone_源参考文�?` 目录不会被提交到GitHub（因为是外部参考资料）�?
如果需要源参考文件，请从原始仓库克隆�?```bash
cd D:\test_Project\owlRD-原型项目
mkdir owdRD_github_clone_源参考文�?cd owdRD_github_clone_源参考文�?git clone https://github.com/原始仓库地址/owlRD.git
```

或者这些文件是可选的，项目可以正常运行，不需要它们�?
---

### Step 1: 安装必要环境

#### Windows系统
```powershell
# 1. 安装Git（如果没有）
# 下载：https://git-scm.com/download/win

# 2. 安装Node.js（如果没有）
# 下载：https://nodejs.org/（推荐LTS版本�?
# 3. 安装Python（如果没有）
# 下载：https://www.python.org/downloads/

# 验证安装
git --version
node --version
npm --version
python --version
```

### Step 2: 克隆项目

```bash
# 进入工作目录
cd D:\test_Project

# 克隆项目
git clone https://github.com/你的用户�?owlRD-prototype.git

# 进入项目
cd owlRD-prototype
```

### Step 3: 恢复后端环境

```bash
# 进入后端目录
cd backend

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境（Windows PowerShell�?.\venv\Scripts\Activate.ps1

# 安装依赖
pip install -r requirements.txt

# 返回项目根目�?cd ..
```

### Step 4: 恢复前端环境

```bash
# 进入前端目录
cd frontend

# 配置淘宝镜像（加速）
npm config set registry https://registry.npmmirror.com

# 安装依赖
npm install

# 返回项目根目�?cd ..
```

### Step 5: 恢复测试环境

```bash
# 进入测试目录
cd tests

# 安装测试依赖
npm install

# 安装Playwright浏览�?npx playwright install

# 返回项目根目�?cd ..
```

### Step 6: 验证环境

```bash
# 测试后端
cd backend
python start_with_check.py
# 应该�?http://localhost:8000 启动

# 测试前端（新终端�?cd frontend
npm run dev
# 应该�?http://localhost:3000 启动

# 测试Vitest（新终端�?cd tests
npm test -- --run
# 应该看到 7/7 passed

# 测试Playwright（需要前端运行）
npx playwright test basic.spec.ts
```

---

## 3. 聊天记录位置

### 📁 聊天记录文件
```
项目文档/聊天记录/
├── 2025-11-22_Vitest和Playwright测试框架集成.md  �?今天的完整记�?└── [其他日期的记�?..]
```

### 📖 如何查看
```bash
# 方法1：用文本编辑器打开
notepad "项目文档\聊天记录\2025-11-22_Vitest和Playwright测试框架集成.md"

# 方法2：用VS Code打开
code "项目文档\聊天记录\2025-11-22_Vitest和Playwright测试框架集成.md"

# 方法3：用Markdown查看�?# 推荐：Typora、MarkText�?```

### 📋 记录内容包含
- �?完整的对话过�?- �?所有遇到的问题和解决方�?- �?代码修改说明
- �?测试结果
- �?技术要�?- �?下次继续的要�?
---

## 4. 环境配置检�?
### �?检查清�?
#### 系统环境
- [ ] Git已安装（`git --version`�?- [ ] Node.js已安装（`node --version` >= 18�?- [ ] Python已安装（`python --version` >= 3.10�?- [ ] npm镜像已配置（`npm config get registry`�?
#### 项目依赖
- [ ] 后端依赖已安装（`backend/venv`存在�?- [ ] 前端依赖已安装（`frontend/node_modules`存在�?- [ ] 测试依赖已安装（`tests/node_modules`存在�?- [ ] Playwright浏览器已安装

#### 服务运行
- [ ] 后端能启动（http://localhost:8000�?- [ ] 前端能启动（http://localhost:3000�?- [ ] Vitest测试通过�?/7�?- [ ] Playwright配置正确

---

## 🚀 快速启动命令（新电脑）

### 一键恢复脚本（Windows PowerShell�?
创建 `setup.ps1`�?
```powershell
# setup.ps1 - 一键恢复项目环�?
Write-Host "开始恢复owlRD项目环境..." -ForegroundColor Green

# 1. 后端环境
Write-Host "`n[1/4] 设置后端环境..." -ForegroundColor Yellow
cd backend
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
cd ..

# 2. 前端环境
Write-Host "`n[2/4] 设置前端环境..." -ForegroundColor Yellow
cd frontend
npm config set registry https://registry.npmmirror.com
npm install
cd ..

# 3. 测试环境
Write-Host "`n[3/4] 设置测试环境..." -ForegroundColor Yellow
cd tests
npm install
npx playwright install chromium
cd ..

# 4. 验证
Write-Host "`n[4/4] 验证环境..." -ForegroundColor Yellow
Write-Host "后端: python backend\start_with_check.py" -ForegroundColor Cyan
Write-Host "前端: cd frontend && npm run dev" -ForegroundColor Cyan
Write-Host "测试: cd tests && npm test" -ForegroundColor Cyan

Write-Host "`n�?环境恢复完成�? -ForegroundColor Green
```

运行�?```powershell
.\setup.ps1
```

---

## 💡 常见问题

### Q1: npm install 很慢怎么办？
```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 或使用代�?npm config set proxy http://127.0.0.1:7890
npm config set https-proxy http://127.0.0.1:7890
```

### Q2: Git推送失败？
```bash
# 如果是首次推送，需要配置用户信�?git config --global user.name "你的名字"
git config --global user.email "你的邮箱"

# 如果是大文件问题
git config http.postBuffer 524288000
```

### Q3: Python虚拟环境激活失败？
```powershell
# PowerShell执行策略问题
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 然后再激�?.\venv\Scripts\Activate.ps1
```

### Q4: 聊天记录丢失�?```bash
# 聊天记录在Git仓库中，确保已提�?git add "项目文档/聊天记录/*.md"
git commit -m "docs: 添加聊天记录"
git push
```

---

## 📌 重要提醒

### 🔐 敏感信息
- ⚠️ 不要提交`.env`文件（已在`.gitignore`�?- ⚠️ 不要提交`node_modules`目录
- ⚠️ 不要提交`venv`虚拟环境

### 📦 必须手动配置�?- npm镜像：`npm config set registry`
- Python虚拟环境：`python -m venv venv`
- Playwright浏览器：`npx playwright install`

### 📝 记得查看的文�?- 聊天记录：`项目文档/聊天记录/2025-11-22_Vitest和Playwright测试框架集成.md`
- 测试指南：`tests/README.md`
- 完成总结：`tests/测试框架集成完成总结.md`

---

## 🎯 迁移成功标志

当你在新电脑上能执行以下操作，说明迁移成功：

```bash
# �?后端启动成功
cd backend && python start_with_check.py
# 访问 http://localhost:8000/docs

# �?前端启动成功
cd frontend && npm run dev
# 访问 http://localhost:3000

# �?Vitest测试通过
cd tests && npm test -- --run
# 看到 7/7 passed

# �?能查看聊天记�?code "项目文档\聊天记录\2025-11-22_Vitest和Playwright测试框架集成.md"
```

---

## 🚀 开始新一天的工作

在新电脑上，每次工作前：

```bash
# 1. 拉取最新代�?git pull

# 2. 启动后端（终�?�?cd backend
.\venv\Scripts\Activate.ps1
python start_with_check.py

# 3. 启动前端（终�?�?cd frontend
npm run dev

# 4. 开始开发！
```

---

## 📞 需要帮助？

查看这些文档�?1. `项目文档/聊天记录/` - 历史对话记录
2. `tests/README.md` - 测试框架使用指南
3. `README.md` - 项目总体说明

**祝在新电脑上开发顺利！** 🎉

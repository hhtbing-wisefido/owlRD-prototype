# 🤖 自动检查机制使用指�?

**创建日期**: 2025-11-26  
**维护�?*: Benson  
**版本**: v1.0  
**规则系统**: v1.1.0  

---

## 📋 目录

- [概述](#概述)
- [方式1: Git Hook自动检查](#方式1-git-hook自动检�? �?最推荐
- [方式2: 文件监控自动检查](#方式2-文件监控自动检�?
- [方式3: VS Code任务集成](#方式3-vs-code任务集成)
- [方式4: Windows定时任务](#方式4-windows定时任务)
- [方式5: 手动检查](#方式5-手动检�?
- [对比与选择](#对比与选择)

---

## 📖 概述

项目结构自动检查机制提�?*5种执行方�?*，可以根据需要选择合适的方案�?

### 检查内�?

�?根目录清洁度  
�?项目文档目录结构�?大分类）  
�?目录编号前缀�?- �?8-�? 
�?重复目录检�? 
�?知识库目录存在�? 
�?目录层级深度（≤4层）  

### 核心脚本

**主检查脚�?*: `scripts/check_project_structure.py`

---

## 1️⃣ 方式1: Git Hook自动检�?�?

**推荐指数**: ⭐⭐⭐⭐�? 
**适用场景**: 所有项目，团队协作  
**触发时机**: 每次 `git commit` �? 

### 特点

- �?**自动触发** - 无需手动运行
- �?**强制执行** - 检查失败阻止提�?
- �?**团队统一** - 所有成员都会执�?
- �?**零成�?* - 安装后自动工�?

### 安装步骤

#### 步骤1: 运行安装脚本

```bash
# 方式1: 使用Python脚本（推荐）
python scripts\install_git_hooks.py

# 方式2: 手动复制（备用）
# 复制 .git\hooks\pre-commit 文件
```

#### 步骤2: 验证安装

```bash
# 查看hook文件
type .git\hooks\pre-commit
```

应该看到�?
```bash
#!/bin/sh
# Git Pre-commit Hook - 项目结构检�?

echo "🔍 运行项目结构检�?.."
python scripts/check_project_structure.py
...
```

#### 步骤3: 测试

```bash
# 创建测试文件
echo test > test.txt
git add test.txt

# 尝试提交（会自动触发检查）
git commit -m "test"

# 清理
git reset HEAD test.txt
del test.txt
```

### 工作流程

```
📝 修改代码
  �?
git add .
  �?
git commit -m "提交信息"
  �?
🔍 自动触发检�?
  �?
�?检查通过 �?允许提交
�?检查失�?�?阻止提交，显示错�?
```

### 跳过检�?

如果确实需要跳过检查：

```bash
git commit --no-verify -m "提交信息"
```

⚠️ **不推�?*经常使用 `--no-verify`

### 卸载

```bash
# 删除hook文件
del .git\hooks\pre-commit
del .git\hooks\pre-commit.bat
```

---

## 2️⃣ 方式2: 文件监控自动检�?

**推荐指数**: ⭐⭐⭐⭐  
**适用场景**: 开发阶段，实时反馈  
**触发时机**: 目录创建/删除/移动�? 

### 特点

- �?**实时监控** - 立即发现问题
- �?**开发友�?* - 边开发边检�?
- ⚠️ **需要运�?* - 需要保持脚本运�?
- ⚠️ **资源占用** - 占用少量系统资源

### 安装依赖

```bash
# 安装watchdog�?
pip install watchdog
```

### 启动监控

#### 方式1: 使用批处理文件（推荐�?

```bash
# 双击运行
启动文件监控.bat
```

#### 方式2: 直接运行Python脚本

```bash
python scripts\watch_and_check.py
```

### 运行效果

```
============================================================
👁�?项目结构监控 - 自动检查模�?
============================================================

📁 监控目录: D:\test_Project\owlRD-原型项目
⏱️ 检查间�? 5�?
🔍 监控事件: 目录创建/删除/移动

💡 提示: �?Ctrl+C 停止监控
============================================================

🔍 初始检�?..
[检查结�?..]

�?监控已启动！正在监控项目变化...

📁 检测到创建: 新目�?
🔍 触发自动检�?- 11:30:00
[检查结�?..]
```

### 停止监控

�?`Ctrl+C` 停止

### 使用建议

- �?开发时保持运行
- �?创建大量目录前启�?
- �?整理项目结构时使�?
- �?不需要一直运行（有Git Hook即可�?

---

## 3️⃣ 方式3: VS Code任务集成

**推荐指数**: ⭐⭐⭐⭐  
**适用场景**: 使用VS Code开�? 
**触发时机**: 手动触发或保存时  

### 特点

- �?**IDE集成** - 方便快捷
- �?**快捷�?* - 可配置快捷键
- �?**终端显示** - 结果直接显示
- ⚠️ **仅VS Code** - 其他IDE不适用

### 配置步骤

#### 步骤1: 复制配置文件

```bash
# 1. 复制示例配置
copy vscode-tasks-example.json .vscode\tasks.json

# 2. 如果.vscode目录不存�?
mkdir .vscode
copy vscode-tasks-example.json .vscode\tasks.json
```

#### 步骤2: 在VS Code中使�?

**方法1: 命令面板**
1. �?`Ctrl+Shift+P`
2. 输入 `Tasks: Run Task`
3. 选择 `🔍 检查项目结构`

**方法2: 快捷�?*
1. �?`Ctrl+Shift+B`（如果设为默认）
2. 自动运行检�?

**方法3: 菜单**
1. 点击 `终端` �?`运行任务`
2. 选择 `🔍 检查项目结构`

### 可用任务

配置文件包含3个任务：

| 任务 | 说明 |
|------|------|
| 🔍 检查项目结�?| 手动运行检�?|
| 🔍 保存时自动检�?| 静默检查（可配置保存时触发�?|
| 🔧 安装Git Hook | 安装Git Hook |

### 高级配置

**保存时自动检�?*:

�?`.vscode/settings.json` 中添加：

```json
{
  "files.saveTaskTrigger": "afterDelay",
  "files.saveTaskDelay": 1000
}
```

---

## 4️⃣ 方式4: Windows定时任务

**推荐指数**: ⭐⭐�? 
**适用场景**: 长期运行的项目，定期检�? 
**触发时机**: 每天固定时间  

### 特点

- �?**完全自动** - 无需人工干预
- �?**定期检�?* - 防止问题累积
- �?**后台运行** - 不影响工�?
- ⚠️ **需要管理员** - 创建任务需要权�?

### 创建定时任务

#### 方式1: 使用脚本（推荐）

```bash
# 以管理员身份运行
scripts\create_scheduled_task.bat
```

**配置**:
- 任务名称: `项目结构检查`
- 执行时间: 每天上午 9:00
- 执行脚本: `check_project_structure.py`

#### 方式2: 手动创建

1. 打开 `任务计划程序`
2. 创建基本任务
3. 触发�? 每天，上�?:00
4. 操作: 启动程序
   - 程序: `python`
   - 参数: `D:\test_Project\owlRD-原型项目\scripts\check_project_structure.py`
   - 起始�? `D:\test_Project\owlRD-原型项目`

### 管理任务

```bash
# 查看任务
schtasks /query /tn "项目结构检�?

# 立即运行
schtasks /run /tn "项目结构检�?

# 删除任务
schtasks /delete /tn "项目结构检�? /f
```

### 查看执行历史

1. 打开 `任务计划程序`
2. 找到 `项目结构检查`
3. 查看 `历史记录` 标签

---

## 5️⃣ 方式5: 手动检�?

**推荐指数**: ⭐⭐�? 
**适用场景**: 临时检查，调试  
**触发时机**: 手动运行  

### 运行方式

#### 方式1: 批处理文件（最简单）

```bash
# 双击运行
检查项目结�?bat
```

#### 方式2: Python脚本

```bash
python scripts\check_project_structure.py
```

#### 方式3: PowerShell

```powershell
& python scripts\check_project_structure.py
```

### 适用场景

- 🔍 整理项目结构后检�?
- 🔍 发现问题后验证修�?
- 🔍 学习和理解检查规�?
- 🔍 生成检查报�?

---

## 📊 对比与选择

### 功能对比

| 方式 | 自动执行 | 实时�?| 资源占用 | 设置难度 | 推荐�?|
|------|---------|--------|---------|---------|--------|
| Git Hook | �?| 提交�?| 极低 | 简�?| ⭐⭐⭐⭐�?|
| 文件监控 | �?| 实时 | �?| 简�?| ⭐⭐⭐⭐ |
| VS Code | ⚠️ | 按需 | 极低 | 中等 | ⭐⭐⭐⭐ |
| 定时任务 | �?| 定时 | 极低 | 中等 | ⭐⭐�?|
| 手动检�?| �?| 按需 | 极低 | 最简�?| ⭐⭐�?|

### 使用建议

#### 个人开�?

```
推荐组合:
1. Git Hook（必备） ⭐⭐⭐⭐�?
2. 手动检查（偶尔使用�?⭐⭐�?
```

#### 团队协作

```
推荐组合:
1. Git Hook（必备） ⭐⭐⭐⭐�?
2. VS Code任务（方便） ⭐⭐⭐⭐
3. 定时任务（可选） ⭐⭐�?
```

#### 大型项目

```
推荐组合:
1. Git Hook（必备） ⭐⭐⭐⭐�?
2. 文件监控（开发时�?⭐⭐⭐⭐
3. 定时任务（每天） ⭐⭐�?
4. CI/CD集成 ⭐⭐⭐⭐�?
```

---

## 🔧 配置优先�?

### 最小配置（必备�?

```
�?Git Hook - 阻止错误提交
```

### 推荐配置

```
�?Git Hook - 提交前检�?
�?VS Code任务 �?手动检�?- 日常检�?
```

### 完整配置

```
�?Git Hook - 提交前检�?
�?文件监控 - 开发时实时检�?
�?VS Code任务 - IDE集成
�?定时任务 - 每日例行检�?
```

---

## 📝 检查报�?

所有检查方式都会生成JSON报告�?

**位置**: `scripts/structure_check_report.json`

**内容**:
```json
{
  "check_time": "2025-11-26T11:30:00",
  "project_root": "D:\\test_Project\\owlRD-原型项目",
  "errors": [
    "�?发现没有编号前缀的目�? 项目文档/聊天记录"
  ],
  "warnings": [],
  "summary": {
    "error_count": 1,
    "warning_count": 0,
    "status": "failed"
  }
}
```

---

## 🛠�?故障排除

### 问题1: Git Hook不生�?

**原因**: hook文件未正确安装或权限问题

**解决**:
```bash
# 重新安装
python scripts\install_git_hooks.py

# 检查文�?
type .git\hooks\pre-commit
```

### 问题2: 文件监控无法启动

**原因**: watchdog库未安装

**解决**:
```bash
pip install watchdog
```

### 问题3: VS Code任务找不�?

**原因**: tasks.json未正确配�?

**解决**:
```bash
# 复制示例配置
copy vscode-tasks-example.json .vscode\tasks.json
```

### 问题4: 定时任务未执�?

**原因**: 权限或路径问�?

**解决**:
1. 检查任务状态：`schtasks /query /tn "项目结构检�?`
2. 查看历史记录找错�?
3. 以管理员重新创建

---

## �?验证安装

### 验证清单

- [ ] Git Hook已安装并测试
- [ ] 手动检查可以运�?
- [ ] VS Code任务配置（如使用�?
- [ ] 文件监控可以启动（如使用�?
- [ ] 定时任务已创建（如使用）

### 测试方法

**测试1: 创建错误目录**
```bash
# 在项目文档下创建无编号目�?
mkdir "项目文档\测试目录"

# 运行检�?
python scripts\check_project_structure.py

# 应该报错: �?发现没有编号前缀的目�? 项目文档/测试目录

# 清理
rmdir "项目文档\测试目录"
```

**测试2: Git提交触发**
```bash
# 创建测试文件
echo test > test.txt
git add test.txt

# 提交（会自动检查）
git commit -m "test"

# 清理
git reset HEAD test.txt
del test.txt
```

---

## 📚 相关文档

- 📄 [项目结构检查脚本](../scripts/check_project_structure.py)
- 📄 [文件监控脚本](../scripts/watch_and_check.py)
- 📄 [Git Hook安装脚本](../scripts/install_git_hooks.py)
- 📋 [项目结构规范检测报告](../7-过程记录/2025-11-26_规则系统全面检测报�?md)

---

**文档版本**: v1.0  
**最后更�?*: 2025-11-26  
**维护�?*: Benson  
**状�?*: �?可用
